<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weekend</title>
   <link rel="stylesheet" href="css\style.css"> 
   <link rel="icon" href="data:,">
   <script src="https://kit.fontawesome.com/1c6c76f476.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="box"id="heading">
   <h1><%= listTitle %> </h1>
   </div>
   <div class="box">
<!--   Ofr loop for future ejs revision however foreach method should be prefered in gneral
    <% for (let i =0;i<newtask.length;i++){%>
        <div class="item">
    <input type="checkbox">
    <p contentEditable="true"><%= newtask[i].name %></p>
    
</div>
   <%}%> -->
   
   <% newtask.forEach(function(tasks) { %>
    <form method="post" action="/delete">
    <div class="item">
        <input type="checkbox"  name="checkbox" onchange="this.form.submit()" value="<%=tasks._id%>" >
        <p contenteditable="true"><%= tasks.name %> </p>
    </div>
    <input type="hidden" name="Todolistname" value="<%= listTitle %>">
</form>
   <% }) %>


   
   <form action="/"  class ="submission" method="post">
    <input type="text" name="Entry" id="entry" placeholder="New task">
    <button type="submit" name="Addtask" value=<%= listTitle  %> >+</button>
   </form>
   </div>
</body>
<%- include("footer");-%> 
</html>